# Mejoras de interfaz para Configuración RMD (Tampermonkey) — v1.28.0

Instalación: en Tampermonkey → "Crear un script nuevo" → pega `rmd-ui-mejoras.user.js` → guarda → recarga el portal.
Solo actúa dentro del iframe de la app (`ui5appruntime.html`). Casi todo es vista; **solo escribe cuando tú lo pides**: "Aplicar" en la vista previa de Pegar y el
**Guardar del propio portal** (en Especificaciones, ese Guardar incluye además los textos y el orden que hayas editado). Lo único que "pulsa" por ti:
"Ir" (Enter en un filtro), el OK de los mensajes de **Éxito** y, si tú lo pides con Ctrl+S, el botón Guardar del diálogo abierto.
Un botón redondo de ajustes (abajo a la izquierda) abre una tarjeta con un interruptor por mejora (agrupadas en *Ventanas y tablas*, *Alertas* y *Herramientas*) y un interruptor general "Mejoras activas".
**El botón es persistente** (v1.9.1): cuelga de `<html>` y no del `<body>`. Tampermonkey ejecuta el script al terminar de leer el HTML, unos segundos *antes* de que UI5 monte la app en el `<body>`; al montarla,
UI5 aparta a una zona oculta cualquier nodo con `id` que cuelgue del `<body>`, y el botón desaparecía. Además se vigila (observador de `<html>` + revisión periódica): si algo lo retira o pierde la hoja de estilos, se vuelve a colgar el mismo elemento con su estado.
Desde la versión 1.9 el script **solo modifica las ventanas del RMD** ("<código> - <descripción>", "Procesos Menores para el Paso…" y los selectores "Adicionar…"); ventanas como **Asociar Fórmula** se dejan exactamente como las dibuja el portal (solo se les añade, si activas esa mejora, un aviso de códigos).

| Mejora | Qué hace |
|---|---|
| Enter = Ir | En filtros y selectores "Adicionar…". No actúa con una lista desplegada ni dentro de la tabla de pasos. |
| Diálogos a medida | **Pasos**: casi pantalla completa. **Estructura, Etiquetas, Procesos menores, Especificaciones, selectores**: centrados y solo con el alto que necesitan. Al cerrar un diálogo hijo (p. ej. Procesos Menores) y volver a Pasos, el cambio de tamaño se suaviza (antes se notaba un salto brusco). *(v1.22)* La **ventana raíz del RMD** ("Estructura de RMD") ya no se cambia: queda con el ancho y las filas del portal (90 % de la pantalla), como la conocen los usuarios. |
| Columnas ordenadas | Anchos por nombre; la descripción toma el resto (nunca menos de 205 px: si no cabe, aparece desplazamiento horizontal en vez de aplastarla). **Depende** y **Tipo Dato** se miden con el texto más largo de cada uno y siempre se ven completos (ya no se corta "Verificación Check", "Realizado por"…). Las cabeceras no parten palabras y ocupan como máximo dos líneas; en ventanas estrechas (< 1500 px) la columna Estado se oculta si todo es "Activo". Sin desplazamiento horizontal desde 1366 px de ancho. |
| Columnas ocultas | Estado Mov., Imagen, Formato, PM OP y Gen PP. **PM OP** *(v1.21)*: no debe marcarse en ningún paso; si alguno la tiene marcada, la columna deja de ocultarse y la celda pide **DESMARCAR PM OP** (cuenta en el aviso de incoherencias). |
| Estado del RMD | Etiqueta (INGRESADO / AUTORIZADO / SUSPENDIDO) **pegada a la derecha** de la cabecera de cada ventana emergente. |
| Título del paso menor | Muestra la descripción completa del paso mayor (hasta 2 líneas; tooltip con el texto entero). |
| "Sin tipo de dato" | Texto en **rojo y negrita** en Tipo Dato. |
| Puesto de Trabajo | Si el combo está habilitado y vacío, la casilla se resalta y **parpadea**. |
| Casillas vs tipo de dato | Marca en la celda qué **MARCAR** (naranja discontinuo) o **DESMARCAR** (rojo) según el tipo: Realizado por → R. Por; Realizado por y Visto bueno → R. Por + V.B.; Visto bueno → V.B.; Notificación y tipos con captura → Edit; Sin tipo de dato / Múltiple check → sin Edit (y sin R. Por, V.B., Estado CC); MuestraCC → Estado CC + Edit. Además: Decimal vacío en tipos numéricos, Rango sin límites, Notificación sin Clave Modelo, Sin tipo de dato con Depende, predecesor colgante o que apunta a un "Sin tipo de dato". Botón "⚠ n incoherencias" salta a la siguiente. |
| **Predecesor obligatorio** | Todo **paso mayor con tipo de dato** (distinto de *Sin tipo de dato*) debe llevar predecesor (celda **Depende**): si está vacía se marca en rojo con el motivo y cuenta en "⚠ n incoherencias". Aplica en Precauciones, Notas importantes, Procedimiento (todas sus etiquetas) y demás listas de pasos. **No aplica** (no se marca) al primer paso con tipo de **Precauciones** (cabeza de la cadena), a las listas que no usan predecesores (**Rendimiento**, **Condiciones ambientales**) ni a los pasos condicionales o en paralelo por su redacción ("EN CASO QUE…", "PARALELAMENTE…", "EN PARALELO…", "BAJO LA SUPERVISION…", "ENTREGAR LA DOCUMENTACION ORDENADA Y FIRMADA…"). El nombre de la lista se lee del modelo del portal (estructura o etiqueta). **Ojo:** el portal vacía el predecesor de la fila en cuanto se marca la casilla **Estado CC**; el aviso lo recuerda (asígnalo *después* de marcarla). |
| Depende con tooltip | "Depende del paso N: <descripción>" (o "colgante"/"sin predecesor"). |
| Tooltips | Significado de Edit, R. Por, V.B., Estado CC, PM OP, Gen PP… al pasar el cursor por la cabecera (sin barras de color). |
| Filtro local | "Filtrar pasos" con contador y botón de incoherencias **siempre visibles** (fijos arriba), igual que el título de la tabla con Guardar y la cabecera de columnas. |
| Ventanas | Todas (incluidos los mensajes) **centradas** y con el pie (Cancelar/Cerrar) siempre visible; no se desbordan de la pantalla. |
| Cambios sin guardar | Avisa antes de Cancelar/Cerrar **solo si tocaste algo** (teclear, casillas, elegir opciones o predecesores) y el estado difiere del último guardado. **Tras Guardar (o Ctrl+S) no vuelve a avisar** aunque el portal refresque o cambie valores después; si el portal responde con una Advertencia o un Error (no se guardó), la ventana vuelve a contar como sin guardar. Marcar filas para copiar y el filtro local no cuentan. El aviso es una **ventana propia centrada en la página** ("Tienes cambios sin guardar": *Descartar y cerrar* / *Seguir editando*; Escape y Enter conservan el trabajo), no el cuadro del navegador. **Ctrl+S** = Guardar. |
| Éxito automático | Cierra solo los mensajes de título "Éxito" con un único OK (900 ms). Confirmaciones y advertencias no se tocan. |
| **Prolongar la sesión** | El aviso del portal "Debido a la inactividad, se finalizará su sesión en N minutos." (que aparece fuera de la app, en el shell de Fiori) se responde solo pulsando **Continuar trabajando**. No se puede alargar el tiempo en sí (lo controla el servidor); esto lo renueva automáticamente cada vez que aparece. *(v1.21)* Además, cada 30 s el propio portal vuelve a buscar la lista principal (su "latido"); si esa búsqueda falla porque la red o la sesión se están recuperando al volver de una ausencia, el portal mostraba un error que había que cerrar. Ahora ese error del refresco **automático** se omite (la vuelta siguiente lo reintenta) si no hiciste clic ni tecleaste desde que empezó; los errores de lo que tú hagas se ven igual. |
| Calidad en Operaciones | El paso mayor "EL PERSONAL DE CALIDAD EN OPERACIONES…" / "CALIDAD EN OPERACIONES REGISTRA…" (Realizado por) debe llevar R. Por + **Estado CC**; los procesos menores que **empiezan** con "CANTIDAD MUESTREADA" o "FECHA / HORA DE MUESTREO" llevan Edit + Estado CC. La verificación del jefe/supervisor ("…VERIFICA EL PROCESO DE MUESTREO DE CALIDAD EN OPERACIONES.") **no** se marca: aunque menciona "muestreo", no le corresponde a Control de Calidad verificarla. |
| Insumos | Procesos menores con Cantidad Insumos / UM: **sin Edit** (si lo tienen marcado se pide desmarcarlo). |
| Textos | Marca la descripción que dice "CONTROL DE CALIDAD" (→ "CALIDAD EN OPERACIONES"), "MUESTRA PARA CONTROL DE CALIDAD" (→ "CANTIDAD MUESTREADA (unidad):") o la nota antigua "…CONTROL DE CALIDAD O CONTROL DE PROCESO…". La redacción vigente **"…APROBACION DE CONTROL DE CALIDAD O CALIDAD EN OPERACIONES, SEGUN APLIQUE"** (nota del granel de Envase; también con las dos áreas al revés) es correcta y **no se alerta**: nombra a Calidad en Operaciones. *(v1.19.1)* Tampoco se alerta el cierre de Acondicionado **"FINALMENTE ENTREGAR EL FORMATO DE INSPECCION EN LINEAS DE PRODUCCION (FPRO-250 VIGENTE) A CONTROL DE CALIDAD PARA SU APROBACION EN EL SISTEMA, ASI COMO EL SOBRE TECNICO…"**; si el mismo paso trae otro "CONTROL DE CALIDAD", ese sí. *(v1.21.1)* Tampoco los **pasos mayores que mencionan BIOCARGA** ("EL PERSONAL DE CONTROL DE CALIDAD MUESTREA (100 mL) PARA ANALISIS DE BIOCARGA, SEGUN LO INDICADO EN EL PROCEDIMIENTO PCMB-200 VIGENTE."): el análisis de biocarga lo hace Control de Calidad. En los procesos menores sí se sigue alertando. |
| **Orden de las estructuras** *(v1.22)* | En la ventana raíz del RMD compara el orden de sus estructuras con el de los **últimos 5 autorizados** de la misma planta, sección y etapa (si hay menos de 2, de la misma planta y etapa). Si alguna está fuera de lugar (por ejemplo, **INSUMOS al final**), marca su Orden en rojo y muestra un aviso con el lugar donde suele ir y los RMD de referencia. Con todo el maestro: ningún Ingresado sale marcado, y con INSUMOS al final se detecta en 252 de 252 casos simulados. En el historial hay 7 autorizados con el orden mal (6 por INSUMOS). |
| **Asociar Fórmula: aviso de códigos** | Al abrir "Asociar Fórmula" compara el **Código Agrupador** y el **Código** con los de la **versión anterior** del mismo RMD (los datos salen de la tabla principal; solo lectura). Muestra una franja **✓ coinciden** o **⚠ no coincide / está vacío** (con el valor anterior) y resalta el campo en ámbar. Si la versión anterior no está en el listado (filtros), lo indica. La ventana conserva su tamaño y aspecto del portal. |
| **Especificaciones: reordenar y editar** | En la ventana de Especificaciones (solo RMD **Ingresados**): **Descripción** y **Especificaciones** pasan a ser cuadros de texto editables (con los máximos del servicio: 150 y 500 caracteres), cada fila tiene un **asa ⠿** para arrastrarla y hay botones **Subir / Bajar** (mueven las filas marcadas una posición). Las filas cambiadas se marcan con una barra ámbar y aparece "● n filas con cambios sin guardar". Se guarda con el **Guardar del propio portal**: sus textos y su orden viajan en la misma actualización de cada fila (entidad `MD_ES_ESPECIFICACION`: `ensayoHijo`, `especificacion`, `orden`), con la conexión del portal. Sin guardar: al cerrar se avisa y se descartan; **Agregar / Eliminar / Ensayos SAP** avisan antes porque releen del servidor. Una Descripción vacía no se guarda. Las especificaciones **importadas de SAP** (`ensayoPadreSAP`) las ordena el portal por su número de característica (`Merknr`), por eso ahí no se reordena (sí se pueden editar los textos). |
| **Copiar / Pegar configuración** | En la ventana de Pasos, en la barra del título de la tabla ("Pasos (n)"), **a la izquierda del icono de impresora**: **Copiar configuración** y **Pegar**. 1) Marca la casilla del paso de referencia y pulsa Copiar (lee su configuración y sus procesos menores). 2) Marca la casilla del paso nuevo y pulsa Pegar: aparece una **vista previa** con lo que cambiará (puedes desmarcar campos y procesos menores) y solo escribe al pulsar **Aplicar**; Escape cierra la vista previa. Copia Tipo Dato, Clave Modelo, Puesto, Val. Inicial/Final, Margen, Decimal y casillas; no copia Orden, Depende, Código ni Descripción. Los procesos menores se agregan **por código** con el selector "Adicionar Pasos RMD", se configuran igual que el origen y se guardan (si ya existen en el destino, no se duplican: se actualiza su configuración). Los **insumos** no se copian (se agregan con "Agregar Insumo"). **El paso copiado es temporal**: se descarta al cerrar el RMD, por eso al abrir otro RMD no queda ningún aviso. Protecciones: no pega en RMD que no estén **Ingresados**, ni sobre el mismo paso copiado, ni en un RMD distinto del copiado. |
| **Copiar / Pegar: novedades v1.21** | Los procesos menores del paso se leen del modelo del portal (al instante y exactos: antes se abría su ventana, que a veces mostraba un momento los de **toda la etiqueta**). **Pegar en varios pasos**: marca uno o varios destinos; la configuración se aplica a todos y se guarda una vez, y los procesos menores paso por paso. La vista previa dice, para cada proceso menor que ya existe en el destino, **qué cambiaría** ("Edit: no → sí") y trae desmarcados los que no cambiarían nada; casilla para marcar o desmarcar todos. El portapapeles tiene un botón **×** para vaciarlo. Al aplicar, en la ventana de procesos menores solo se miran las filas de ese paso. |
| **Copiar más rápido** *(v1.23)* | Si los combos de la tabla aún no traían el nombre de algún Tipo de Dato, Copiar abría la ventana de procesos menores para leerlo (varios segundos). Ahora usa los nombres conocidos del portal y el tipo del paso maestro: ~0,2 s por paso, sin abrir ventanas. |
| **Nuevo Paso desde "Adicionar Pasos"** | En el selector "Adicionar Pasos" (donde eliges pasos ya existentes para añadirlos al RMD), junto al botón **Ir**: botón **Nuevo Paso**. Abre, apilada encima, la misma ventana "Nuevo Paso" de **Configuración Maestra** (cambia sola a la pestaña "Paso" y precarga Estructura/Etiqueta con el contexto del selector). Tú completas el resto y pulsas **Agregar** ahí mismo; al cerrar esa ventana vuelves al selector para buscar el paso recién creado por su código y añadirlo al RMD. |
| **Ver OP: ver todas, filtrar y exportar** | En "Ver OP" (Ordenes de Producción Asociadas), que el portal siempre pagina de 5 en 5 (vuelve a leer del servidor con cada flecha "▷"; cada página hace 10 llamadas propias del portal — RMD, receta, estructura, equipo, paso…, no es un límite de pantalla ni algo que se pueda acelerar sin inventar llamadas propias): botón **Ver todas** (recorre esas páginas con la misma flecha, junta todo sin duplicados y muestra el progreso "Cargando… N"; con historiales muy largos pregunta cada cierto tramo si seguir). Cada columna (Nro., Versión, Código, Descripción, Lote, Fechas, Estado…) tiene un **icono de embudo** junto al encabezado, igual que el Autofiltro de Excel: abre un desplegable con los valores que aparecen en esa columna (con buscador y casillas, "Todo"/"Ninguno"), y desmarcar valores oculta esas filas; se combinan entre columnas. El icono se resalta cuando esa columna tiene un filtro activo. El botón **Exportar a CSV** descarga todas las OP asociadas (primero las carga todas, como "Ver todas"). *(v1.18: se retiró la opción "Recetas del RMD", que ya no se usa.)* |
| **Ver todas las OP, rápido** *(v1.21)* | "Ver todas" y "Exportar a CSV" hacen las mismas 10 lecturas por página que el portal (misma entidad, filtros, tamaño de página y orden; las filas quedan con todo lo que usan "Ver datos OP", "Ver Registros" y "Desasociar"), pero en paralelo y de 3 páginas a la vez, con una barra de avance y **Detener** en vez del aviso de 8 s por página: 58 OP en 6 s (antes ~1,5 min). Si algo falla, usa la paginación del portal como antes. |
| **Enviar a Status RMD** *(v1.17)* | Botón junto al icono nativo **"Exportar"** de la lista principal. Lee aquí mismo, con la sesión ya iniciada, el maestro completo (todos los RMD, todas sus versiones y todas sus recetas asociadas, leídos por páginas de 1000 porque el servicio no entrega más por consulta) y se lo pasa a **Status RMD** (`status-rmd.vercel.app`), que se abre en otra pestaña, **sin descargar ni subir archivos**. Los datos van de una pestaña a otra del mismo navegador con `postMessage`, restringido a los orígenes exactos de ambas páginas; no se envía nada a ningún servidor nuevo ni se usa ninguna credencial. Desde **v1.19** el envío lleva también el **usuario con el que se inició sesión en el portal** (nombre, correo e identificador, leídos del propio launchpad con `sap.ushell.Container.getUser()`; nunca una contraseña) y Status RMD registra la sincronización con él: **un solo clic, sin DNI** (si el launchpad no da el usuario, o con una versión anterior de Status RMD, se pide el DNI como antes). El botón muestra el progreso ("Leyendo SAP… n/13", "Procesando en Status RMD…") y un aviso final con el resultado y **qué cambió** desde la última sincronización (etapas autorizadas en SAP, nuevas pendientes, con otro RMD o estado, autorizaciones manuales caducadas). Si el navegador bloquea la ventana emergente, avisa para permitirla en el portal. No escribe nada en SAP. *(v1.18: envía también la hora de registro, en columnas aparte que una versión anterior de Status RMD simplemente ignora, para ordenar bien dos RMD registrados el mismo día.)* |
| **Menú Exportar** *(v1.23)* | El icono nativo **"Exportar"** de la lista principal abre un menú con: **Exportado original** (el mismo Excel del portal, "RMD_Configuracion", con los filtros aplicados y las 18 columnas de siempre, más **"Producción Estado"** al final —PENDIENTE, ENVIADO, APROBADO, RECHAZADO— para no mover las demás), **Equipos por master**, **Indicadores del mes** y, desde v1.24, **Documentos citados en todos los master**. Los botones sueltos de Indicadores y Equipos se retiraron. |
| **Documentos citados en todos los master** *(v1.24)* | En el menú Exportar. Lee el catálogo de pasos (~91 mil), se queda con los que citan un documento (<I/P/F><Área>-<sufijo>) y trae solo sus apariciones en los master de los estados elegidos (por defecto todos menos Suspendido y Cancelado), como paso o como proceso menor. Excel con **Documentos** (en cuántos master y cuántos autorizados está cada uno), **Citas** (documento, RMD, estado, etapa, área, planta, lista, paso/proceso menor y texto) e **Información**. Autorizados + Ingresados: 740 documentos, 83 074 citas en 2 149 master, ~50 s. |
| **Indicadores del mes** *(v1.20)* | En el menú del icono **"Exportar"** (antes, botón suelto). Elige el mes (por defecto el anterior durante la primera quincena) y, opcionalmente, el archivo del mes anterior; lee aquí el maestro completo de SAP (la misma lectura de "Enviar a Status RMD", unos segundos) y descarga **`BD RMD <MES> <AÑO> - P1-P2.xlsx`** con las mismas hojas y tablas dinámicas que el equipo arma a mano: *Exportación SAPUI5* (las 18 columnas del Exportar nativo + las 12 calculadas con sus mismas fórmulas), *PEND PL1*, *PEND PL2*, *RESUMEN* (las 7 tablas dinámicas, en las mismas celdas y con los mismos filtros) y *Hoja1*. Asigna el **A/F** con la regla del equipo y **sugiere "No contar"** (marcado en amarillo claro, con el motivo en "Por revisar"). Nada se envía a ningún servidor. Detalle en [`docs/indicadores_rmd.md`](../docs/indicadores_rmd.md). |
| **Equipos por master** *(v1.22)* | En el menú del icono **"Exportar"** (v1.23; antes, botón suelto). Lee de SAP todos los master y sus **EQUIPOS / INSTRUMENTOS / MATERIALES** (las mismas entidades que usa el portal al abrir esa estructura: equipos, utensilios y agrupadores de utensilios) y descarga "Equipos por master RMD <fecha>.xlsx" con 4 hojas en tablas de Excel con filtro: **Equipos por master** (una fila por master y equipo; primero Código equipo, Equipo, Tipo, N.º SAP y Ubicación técnica, y luego Código RMD, Versión, Descripción del master, Código por defecto, Estado, Área (sección), Etapa, Planta, Fecha autorización y Orden), **Resumen por equipo** (en cuántos master está cada uno), **Masters** (cuántos equipos tiene cada master) e **Información**. Antes eliges los estados (por defecto todos menos Suspendido y Cancelado). Autorizados + Ingresados: 2148 master, 20 682 filas, ~17 s. |
| **Filtro "Equipo"** *(v1.28; antes botón "Buscar por equipo")* | Una tarjeta más de la barra de filtros de la lista principal, junto a las del portal: escribe el **código o parte del nombre** de un equipo, instrumento, material, utensilio o agrupador (con **sugerencias** del catálogo mientras escribes) y pulsa **Ir**: la lista muestra solo los master que lo tienen, **combinado con los demás filtros** (Planta, Etapa, Estado…). Por dentro hace la misma lectura de la lista del portal, con sus filtros, añadiendo los master que tienen ese equipo. **⟳** (Restablecer) lo vacía. Ej.: PL1-LIQ-E023 → 117 master no cancelados en ~4 s. La búsqueda con el detalle por equipo y el Excel sigue en **Ctrl+K → "Buscar por equipo (detalle y Excel)"**. |
| **Barra de filtros compacta** *(v1.28)* | "Código Agrupador" pasa a **"Agrupador"** (se usa poco), **Restablecer** pasa a un **⟳ rojo** y las tarjetas se estrechan para que entren **todas en una fila** (10 en una pantalla de 1920). La columna **Etapa** de la lista se ensancha para que "ACONDICIONADO" quede en una sola línea. |
| **Modificaciones masivas** *(v1.28; antes "Suspensión masiva")* | Desde el **panel de mejoras** (botón rojo "Modificaciones masivas…") o **Ctrl+K**: ya no está en la barra de la lista, porque se usa poco y cambia muchos master a la vez. Dos pestañas: **Suspender** — master **Autorizados o Ingresados** (el portal ofrece "Suspendido" a ambos), con el motivo opcional como una línea nueva al final de Observaciones; **Agregar observación** — una línea nueva al final de Observaciones de master en **cualquier estado** salvo Cancelado (el portal no guarda nada en un master cancelado), sin cambiar el Estado. Pegas los códigos → **Revisar** dice cuáles aplican según su estado → el botón rojo pide confirmación y, para cada uno, hace lo mismo que a mano: abre su **"Asociar fórmulas"**, pone el Estado (si se suspende) y la línea en Observaciones, y pulsa el **Guardar del portal** (sus validaciones, la trazabilidad y, al suspender, la anulación del documento de cada receta en el DMS). En un Autorizado de Fabricación o Envase el portal compara antes las cantidades de insumos y, si difieren, abre el "Comparador de fórmula" en lugar de guardar: se informa en el resultado. Comprueba en SAP que quedó hecho, se puede **Detener** y **Exportar Excel** con el detalle (modificación, estado anterior y actual, resultado, línea agregada, observaciones, fecha y usuario). Suspender necesita el permiso para cambiar el Estado (rol Jefe DT). |
| **Recetas desactualizadas** *(v1.24; v1.27: detalle y al día)* | Al asociar una receta, el portal copia su lista de materiales de SAP en el RMD y esa copia ya no cambia. El script la compara con lo que devuelve hoy SAP (la misma lectura del portal: MaterialSet por material, planta y alternativa) y, si cambió, **avisa** en la ventana raíz del RMD y en **Asociar fórmulas** con un resumen corto ("3 reemplazados por otra versión del material, 2 nuevos"). *(v1.27)* Junto al **código** de cada receta con cambios aparece **⚠ n**: al pasar el ratón (o con clic, para dejarlo abierto) muestra una tabla con cada componente, su descripción, la cantidad **en el RMD → en SAP hoy** y el tipo de cambio: **≠** otra cantidad o unidad (con la diferencia), **⇄** reemplazado por otra versión del material (p. ej. "CJA … x25" → "CJA … x25 H v.1", con el código anterior tachado), **+** nuevo y **−** quitado. *(v1.28)* El aviso de arriba es solo el resumen ("⚠ Lista de materiales actualizada en SAP en 1 receta(s): 6000003527 / 1101"); el detalle está en el ⚠ junto al código o con clic en el código del aviso. El aviso **sigue a la tabla de recetas**: al eliminar la receta desactualizada (o agregar otra) se rehace solo, sin cerrar la ventana (antes seguía mostrando la receta eliminada hasta volver a abrir Asociar fórmulas). Para traer la nueva: Eliminar Receta → Agregar Producto → asociarla de nuevo. **Solo avisa: no impide autorizar.** Botón **Revisar recetas** para comprobarlo a pedido. |
| **Recetas: hoja de ruta y puesto de trabajo** *(v1.27, apagado por defecto)* | Actívalo en el panel (Alertas). Compara también la versión de fabricación asociada con la de SAP hoy (ProduccionVSet) y avisa solo si cambió lo más resaltante: **puesto de trabajo (línea)**, **hoja de ruta**, **contador**, **alternativa de la lista de materiales**, si la versión **ya no existe**, o si los **puestos de la hoja de ruta** (la misma lectura del combo "Puesto Trabajo" del portal) ya no incluyen alguno que usan los pasos del RMD. El detalle (en el mismo ⚠) muestra cada dato **asociado → en SAP hoy** y, si hay versión anterior del RMD, lo que usaba esa versión; el estado de bloqueo, la validez y el tamaño de lote van como información (el bloqueo cambia a menudo y el portal ya lo refresca con su botón). |
| **Saludo** *(v1.26)* | Al entrar, abajo a la izquierda (junto al botón de mejoras) y sin tapar nada: **"Buenos días / Buenas tardes / Buenas noches, <nombre>"** (el nombre del usuario del launchpad), cuántos RMD tuyos hay en Ingresado (y cuántos en revisión o con la revisión rechazada) y **"Continuar con <el último RMD que abriste>"**, que abre su configuración. Se desvanece solo a los ~7 s (si pasas el ratón por encima, espera). No se repite si recargas la página en menos de 10 minutos. |
| **Ir a… (Ctrl+K)** *(v1.26)* | Desde cualquier pantalla del portal, **Ctrl+K** abre un buscador: escribe un **código** o parte de la **descripción** y **Enter** abre "Configurar el RMD" (filtra la lista y usa el mismo menú de la fila, como a mano); **Alt+Enter** abre Asociar fórmulas, y en cada RMD hay botones **Fórmulas**, **Master** y **Filtrar**. Muestra primero tus **recientes** (los RMD abiertos en este navegador), luego **tus RMD en Ingresado** (registrados o actualizados con tu usuario) y las **herramientas** del script (Buscar por equipo, Suspensión masiva, Exportar…, Equipos por master, Indicadores, Documentos citados en todos los master, Enviar a Status RMD, panel de mejoras). ↑/↓ para elegir, Esc para cerrar. Con una ventana del portal abierta solo filtra la lista (para abrir otro RMD, primero cierra las ventanas). |
| **Título de la pestaña** *(v1.26; v1.28: etapa abreviada)* | Con un RMD abierto, la pestaña del navegador muestra su **etapa abreviada** (FAB, ENV, ACO, INS, REC) y su descripción separadas por un guion ("FAB - TRAMEDIF 100 mg INY (100 L) — Configuracion RMD"): útil con varias pestañas del portal. Al cerrarlo vuelve el título normal. |
| **Solo en la lista principal** *(v1.28)* | Los botones del script de la barra de la lista (Enviar a Status RMD, menú Exportar) ya no aparecen en **Configuración Maestra** ni en otras ventanas del portal que también tienen un botón "Exportar". |
| **Teclado sobre ventanas del portal** *(v1.26)* | UI5 devolvía el foco a la ventana de SAP abierta cuando se pulsaba en un cuadro propio del script (no se podía escribir en él). La paleta, los avisos propios, el panel y el menú Exportar se registran en UI5 como contenido externo de sus ventanas, así que ahora funcionan encima de ellas. |
| **Adicionar Pasos: un paso varias veces, en orden** *(v1.25; reemplaza el "Agregar varias veces" de v1.24)* | En la barra de pasos marcados (arriba de la tabla), cada paso lleva un **+** que pone otra copia justo a su lado: para 4 veces el paso 2 → `3 | 2 | 2 | 2 | 2 | 204`. Los pasos de la barra se pueden **arrastrar** para cambiar el orden, y **Agregar** los agrega en el orden de la barra, uno por elemento (es lo que ya hacía el Agregar del portal: solo faltaba que la barra admitiera repetidos). Marcar o desmarcar otras filas ya no quita las copias; la **×** quita una copia y la fila sigue marcada mientras quede otra; desmarcar la fila quita todas sus copias. Tras Agregar, la ventana se cierra como siempre. Se retiró la nota que v1.24 ponía en el pie, que empujaba **Agregar** fuera de la pantalla. La ventana es más ancha (1480 px en 1920) y **Ir** y **Nuevo Paso** quedan en la misma fila que los filtros. |
| **RMD en vivo** *(v1.24, apagado por defecto; v1.25: va a lo que cambió)* | Actívalo en el panel de mejoras. Con la configuración de un RMD abierta, la **mitad derecha** muestra el PDF del RMD (el mismo que genera el portal con Imprimir / Ver master) y se regenera solo después de cada cambio guardado (o con **Actualizar**); las ventanas de configuración pasan a la **mitad izquierda**. *(v1.25)* Tras cada cambio el PDF **se abre en la página y la altura de lo que se agregó o modificó**, lo **resalta en amarillo** unos segundos y luego se funde con la versión normal (si hiciste clic dentro del PDF, no se le mueve: te quedas donde estás). Cada versión se carga detrás y aparece con un fundido, sin pantalla en blanco; una barra fina indica que se está generando, y si el documento no cambió no se recarga. **Ir al cambio** vuelve a llevar al último cambio. El visor abre sin la columna de miniaturas, para leer mejor. Cómo sabe qué cambió: compara el documento de pdfMake con el anterior por bloques de texto (ignorando la renumeración de los pasos que siguen a uno nuevo) y pdfMake dice en qué página y altura quedó cada bloque. Al cerrar el RMD todo vuelve a su lugar. |
| **Editar Paso usado en otros RMD** *(v1.25)* | Al **Grabar** en "Editar Paso", el script mira en qué otros RMD está ese paso (como paso o como proceso menor). Si está en otros en proceso (Ingresado u otro estado abierto) o ya existe otro paso con la misma descripción, pregunta antes: **Generar un nuevo paso** (verde, recomendado: solo este RMD usa el texto nuevo; si ya existe un paso con esa descripción, lo **reutiliza** en vez de crear un duplicado con otro código) o **Sobrescribir el paso** (ámbar: el cambio se ve en los demás RMD; desactivado si alguno está Autorizado o Suspendido, porque el portal no lo permite). Luego una confirmación que **se acepta sola a los 5 s** (Cancelar la detiene). Sobrescribir sigue con la confirmación propia del portal. Si el paso no está en otros RMD ni duplica otro, Grabar funciona como siempre. |
| **Fórmulas: reordenar términos** *(v1.25)* | En la ventana "Fórmulas", botones **Subir** / **Bajar** (o Alt+↑ / Alt+↓) mueven el término marcado sin tener que eliminarlo y volver a ingresarlo; el texto de la fórmula se rehace al momento y **Guardar** graba el nuevo orden (el portal guarda el orden según la posición). |
| **Producción Estatus: a quién se envió** *(v1.25)* | En la lista principal, bajo "Producción Estatus", el **nombre y apellido del jefe** (y del gerente, si lo hay) a quien se envió a revisión, leídos de los destinatarios del RMD y del catálogo de usuarios del portal. |
| **Asociar fórmulas: varias recetas y puesto de trabajo** *(v1.25)* | Las recetas asociadas se pueden **marcar varias** y **Eliminar seleccionadas (N)** las quita de una vez, con una sola confirmación, haciendo lo mismo que el botón de cada fila (y la anulación de su documento en el DMS si el RMD está autorizado). Al **asociar** recetas, si alguna tiene un **puesto de trabajo** distinto al de las ya asociadas (o las elegidas no coinciden entre sí), no deja continuar y dice cuáles. |
| **Documentos citados** *(v1.23)* | En la ventana raíz de un RMD: botón **Documentos citados**. Solo los documentos (instructivos, procedimientos y formatos con el patrón <I/P/F><Área>-<sufijo>, ej. IPRO-P123) citados en **todos** los pasos y procesos menores del RMD, con dónde aparece cada uno, y descarga a Excel (Resumen, Documentos citados, Citas). Desde v1.23 se lee del modelo del portal en 3 lecturas, sin abrir ventanas: menos de 1 s (antes de 20 s a varios minutos), y ya no junta incoherencias (esas se ven en cada lista de pasos). |
| **Procesos menores sin abrirlos** *(v1.21)* | En la lista de pasos, los procesos menores de todos los pasos se revisan en segundo plano (del modelo del portal, sin abrir ninguna ventana) con las **mismas reglas** que se ven al abrirlos. El paso con alguno mal configurado muestra en la celda **Proc. Men.** un contador rojo con cuántas incoherencias tiene y, en su tooltip, cuáles ("Proceso menor 2 (CLOROCRESOL): DESMARCAR Edit"); cuentan en el aviso de la lista e "ir a la siguiente" se detiene ahí. Al abrir el proceso menor se ve el detalle en cada celda, como siempre. Se vuelve a revisar al cerrar una ventana de procesos menores. "Documentos citados" usa la misma lectura: **19 s en vez de 3 min** en un RMD de 101 pasos y 152 procesos menores. |
| **Asociar Fórmula: nomenclatura de Observaciones** | Valida el **primer "token"** (hasta el primer espacio) de la 1ª línea de Observaciones contra `AAAAMMDD` + 2 iniciales `-prioridad(1-3)` `-C`complejidad **(siempre con 1 decimal: 0.5/1.0/1.5/2.0/2.5/3.0)** `-FI1.0` `-FA1.0` `-F`fase (un dígito + "R" opcional, con o sin guion: F1, F1R, F2, F2-R, F0…) — ej. `20260918CJ-1-C2.0-FI1.0-FA1.0-F2`. Lo que venga después de un espacio (otra nota en la misma línea) no se evalúa ni cuenta como "sobra". Muestra ✓ con el resumen si es correcta, o ⚠ señalando **qué parte** falla y resalta el campo. |
| **Predecesor: aviso adicional** | *(ver la fila de arriba, en "Predecesor obligatorio")* — si a la fila sin predecesor ya se le marcó Estado CC, el aviso añade que el portal vacía el Depende al marcarla: asignarlo después. |
| **En minúsculas** *(v1.21)* | Botón en la barra de Pasos y de Procesos menores. Marca un paso en MAYÚSCULAS y pulsa **En minúsculas**: una vista previa muestra la descripción redactada (mayúscula inicial, tildes, unidades como mL o °C, códigos como FPRO-201 o PV1-PHM-09 tal cual y punto final: "ADICION DE CLOROCRESOL.-" → "Adición de clorocresol.") —editable— y todo lo que se copia del paso maestro (estructura, etiqueta, numeración, tipo de dato, clave modelo, valores, decimales y lapso), avisando si en este RMD está configurado distinto (con opción de usar esa configuración). Luego abre **"Nuevo Paso"** de Configuración Maestra con todo ya puesto: tú revisas y pulsas **Agregar** (el script no guarda nada). *(v1.21.1)* Conserva "pH", "mL" y demás unidades ya escritas así, el código completo (IPRO-P202), siglas como POE y las áreas con mayúscula (Calidad en Operaciones, Control de Calidad, Aseguramiento de la Calidad). Si el texto solo cambia de mayúsculas a minúsculas, avisa que al pulsar Agregar el portal preguntará "¿Desea crear o actualizar el Paso?": **Sí** crea el paso nuevo y el original no cambia. En Rango recuerda que el portal exige Valor inicial, Valor final y Margen. |
| **"Aa" minúsculas con redacción** *(v1.23: ya no es experimental; va con la opción "Pasar a minúsculas", activa por defecto)* | En cuadros de texto en MAYÚSCULAS editables (se detectan por el campo, no por el título de la ventana: Descripción Paso en "Nuevo Paso" y en "Editar Paso" —de un paso mayor o menor— cuando el portal la deja editar, Descripción/Especificaciones de Especificaciones), para **cualquier Tipo Dato** (el botón aparece en cuanto el portal habilita el campo, ya no depende de que ya estuviera habilitado al abrir la ventana): botón **Aa** que convierte a minúsculas con mayúscula al iniciar oración y después de punto, restaura tildes de un diccionario de palabras frecuentes (según, también, verificación, técnico, válvula, rótulo…) y conserva los códigos de referencia (IPRO-P123…) y siglas conocidas (RMD, CC, UM, POE…). *(v1.21.1)* Aparece también cuando el texto en MAYÚSCULAS lleva unidades o símbolos en minúsculas ("RESULTADO DE pH", "(100 mL)"): antes una sola minúscula lo ocultaba y esos pasos se quedaban sin el botón. En la Descripción Paso redacta igual que "En minúsculas". En "Editar Paso" recuerda que el portal no deja grabar un paso que ya está en RMD autorizados ("El paso se encuentra en RMDs Autorizados no se puede actualizar."): para esos, "En minúsculas" crea un paso nuevo. No aparece en campos de solo lectura (por ejemplo, "Editar Paso" de Configuración Maestra abre la Descripción Paso sin poder editarla: el portal no lo permite ahí). Es una heurística, no un corrector real: revisa el resultado antes de guardar. |

## Estilo
Diseño minimalista para no romper la costumbre del usuario de la interfaz original: usa la tipografía y la paleta del propio tema Fiori
(oscuro por defecto; cambia a claro si el portal cambia de tema), botones con contorno fino y texto de acento (relleno solo en "Aplicar"),
casillas a marcar/desmarcar **encerradas** (recuadro discontinuo ámbar = marcar, sólido rojo = desmarcar), etiquetas de estado con contorno, un aviso de incoherencias en texto
discreto, animación lenta (2,4 s) en el Puesto de Trabajo faltante —se desactiva si el sistema pide reducir movimiento— y un botón de ajustes pequeño.
Las tablas siguen siendo las del portal.

## Apagar mejoras
Cada casilla del botón de ajustes retira lo que esa mejora añadió (botones, barra, etiqueta de estado, columnas ocultas, anchos); con "Mejoras activas" apagado
la ventana vuelve a verse exactamente como la del portal.
